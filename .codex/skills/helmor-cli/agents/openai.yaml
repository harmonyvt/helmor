interface:
  display_name: "Helmor CLI"
  short_description: "Use Helmor from the terminal, including managed workspaces."
  default_prompt: "Use the Helmor CLI to inspect data, create/import managed workspaces, send prompts, or run Helmor as an MCP server."
