interface:
  display_name: "Helmor CLI"
  short_description: "Use Helmor from the terminal, including managed workspaces and Debug ingest."
  default_prompt: "Use the Helmor CLI to inspect data, create/import managed workspaces, run Helmor debug commands, manage Debug ingest forwarding, send prompts, or run Helmor as an MCP server."
