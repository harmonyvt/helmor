interface:
  display_name: "Helmor CLI"
  short_description: "Use Helmor from the terminal, including managed workspaces and ngrok forwarding."
  default_prompt: "Use the Helmor CLI to inspect data, create/import managed workspaces, manage Debug ingest ngrok forwarding, send prompts, or run Helmor as an MCP server."
